# Workspace docs

Project-specific documentation is now colocated with each mod:

- `SlowToxVisualized/docs/` for `slowtoxvisualized`
- `PlayerStatusStrip/docs/` for `playerstatusstrip`

Keep this root `docs/` folder only for cross-mod workspace notes.
